[main]

capslock = overload(control, esc)
control = capslock
tab = overloadt2(nav, tab, 220)
# space = overloadi(space, timeout(overloadt2(nav, space, 200), 500, space), 125)

[nav]
h = left
j = down
k = up
l = right

e = end
a = home
p = up
n = down
b = left
f = right

; = pagedown
d = delete
